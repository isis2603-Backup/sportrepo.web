define(['controller/_countryController','delegate/countryDelegate'], function() {
    App.Controller.CountryController = App.Controller._CountryController.extend({

    });
    return App.Controller.CountryController;
}); 