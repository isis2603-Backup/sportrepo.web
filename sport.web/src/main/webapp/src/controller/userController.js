define(['controller/_userController','delegate/userDelegate'], function() {
    App.Controller.UserController = App.Controller._UserController.extend({

    });
    return App.Controller.UserController;
}); 