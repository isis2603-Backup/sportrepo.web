define(['controller/_addressController','delegate/addressDelegate'], function() {
    App.Controller.AddressController = App.Controller._AddressController.extend({

    });
    return App.Controller.AddressController;
}); 